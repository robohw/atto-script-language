; a simple loop with LineFeed 
; B + equal B = B + 1
; ---------------------------

TRC                 ; trace turn ON
B = 0
T = 10              ; LineFeed (ASCII)

.LOOP               ; LOOP label
 B + 
 PRN B T
IF B < 5 JMP .LOOP
